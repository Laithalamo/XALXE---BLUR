{
  "name": "xalxe",
  "private": true,
  "version": "0.1.0",
  "description": "XALXE - browser combat racing game (private LAN play)",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared",
    "mimoza"
  ],
  "scripts": {
    "prestart": "npm install --no-audit --no-fund --loglevel=error",
    "start": "npm run build && npm run start -w server",
    "dev": "npm run dev -w client",
    "build": "npm run build -w client && npm run build -w mimoza",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server && npm run typecheck -w mimoza",
    "textures": "python3 tools/textures/generate.py assets/textures"
  },
  "engines": {
    "node": ">=20.19"
  }
}
